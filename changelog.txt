-fixed issue where placeholders wouldn't work properly
-added new info hover popup for Listening state for the Big Image Text
-changed that you can now leave the Big/Small Image Texts empty
-changed the default YouTube Music state to have the Big Image Text empty
-removed "Dynamic Tags" section in warning hovers
-fixed that "Native App Not Installed" popups never was shown
-fixed that "Native App Not Installed" now show once per tab (if you press it away, it won't show anymore on this tab)
-fixed that "Native App has an Update" popups never was shown
-fixed that "Native App has an Update" now show once per tab (if you press it away, it won't show anymore on this tab)
-added an option in the popup to mute "Native App has an Update" alltogether
-added "Newer" and "Older" version seperation for Native App Updates
-temporarily disabled "Playing" Activity Type due to a bug making it not work anymore
-added a small warning hover to show (why) "Playing" Activity Type is disabled
-updated README.md

-cleaned up the folder a little bit
-updated and made build.bat public
-removed some unnecessary files
-updated icon.ico
-changed setup.iss so it actually works when you switch the directory during installation


known bugs:
-playing state doesn't work at all currently
-when a YouTube video is paused, it sometimes randomly stops counting for how long